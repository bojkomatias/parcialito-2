<p id="notice"><%= notice %></p>

<h1>Vaccines</h1>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th>Cantidad de Applicaciones</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @vaccines.each do |vaccine| %>
      <tr>
        <td><%= vaccine.name %></td>
         <%# total = 0%>
          <%# vaccine.users.each do |v|%>
              <%# total += 1%>
          <%# end %>
          <td><%#= link_to total, appointments_path(:id => id)%></td>
        <td><%= link_to 'Show', vaccine %></td>
        <td><%= link_to 'Edit', edit_vaccine_path(vaccine) %></td>
        <td><%= link_to 'Destroy', vaccine, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Vaccine', new_vaccine_path %>
